# Instruction — Project Agent Wrangler Configuration

Purpose: isolate the Project Agent Worker from production chat.
Required: project repo, worker entrypoint and agent version remain explicit.
Do not add production deployment bindings without separate approval and verification.
Status: ACTIVE — v1.1 candidate.
