# Instruction — backend/project-agent/wrangler.toml

This file defines the isolated Project Agent Worker. Production chat Worker configuration must remain separate. Version changes require isolated runtime verification before promotion.
