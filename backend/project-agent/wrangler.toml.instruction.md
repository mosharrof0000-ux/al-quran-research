# Wrangler Instruction

This configuration belongs only to the isolated Project Agent Worker. Keep production chat Worker configuration separate. Required secrets are runtime secrets and must never be committed: GEMINI_API_KEY, GITHUB_TOKEN, AGENT_ACCESS_TOKEN. Deployment remains a later governed gate.
