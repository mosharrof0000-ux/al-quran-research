# Folder Master — backend

Purpose: govern and index backend files, runtime, API and Worker dependencies.

Protected: live connection paths, API contracts, Worker entrypoints, recovery behavior.

Organization rule: inventory and dependency-check before any move/rename.