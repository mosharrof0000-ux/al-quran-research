# Folder Master — data

Purpose: govern research datasets and structured data files.

Protected: raw data, stable research IDs, versioned records. Never overwrite raw research data silently.

Organization rule: map schema, instructions, IDs and consumers before changes.