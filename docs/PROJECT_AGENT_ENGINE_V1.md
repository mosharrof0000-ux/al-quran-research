# Project Agent Engine v1.1 — Isolated

## Purpose
The Project Agent is a project-aware autonomous work engine foundation: inspect → understand → plan → isolated edit → verify → handoff.

## Capabilities
- Bengali work-role Agent identity
- Task ID / Agent ID / Session ID
- isolated agent/* branch
- project file/directory inspection
- code search
- branch status inspection
- bounded Gemini tool loop
- task/work-history and handoff records
- protected-path rules

## Safety boundary
Production chat Worker and main remain untouched. Merge and deployment are outside the agent runtime. Protected workflow/database/migration/validation assets remain blocked.

## Promotion gate
Agent branch → review → validation → explicit approval → main promotion → deployment verification → live smoke test.

## Current task
শামীম / SHAMIM-001 / AI-STRENGTHENING-001.
