# Instruction — Agent Identity Registry
Status: ACTIVE
Purpose: Persistent identity rules for project work history.
Rules: do not erase prior identities; each task must remain traceable; identity does not represent a real person.
Verification: every task record must expose Task ID, Agent ID, Session ID, work type and branch.
