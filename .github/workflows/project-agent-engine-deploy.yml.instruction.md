# Instruction — Project Agent Engine deployment workflow

This workflow may deploy only the isolated Project Agent Worker. It must not modify or deploy the production chat/reader Worker. Required Cloudflare credentials remain GitHub/Cloudflare secrets and must never be written to the repository.
